---
name: My Workflow
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Replace
        id: test
        uses: powerops-mk/replace-action@main
        with:
          filter: "*.*"
          path: ./Test
          find: "XX_IP"
          replace: "ZYX"

      - name: Get the output
        run: echo "The ${{ steps.test.outputs.count }}"
